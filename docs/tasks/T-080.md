# T-080: E2E: Rollback

- **Status**: DONE
- **Milestone**: 24 — E2E Tests: Pipeline & Analysis
- **Depends**: T-059
- **PRD Reference**: §11.2
- **Completed**: 2026-03-10 14:22
- **Commit**: 27747cc
- **Cost**: $13.75

## Description

Create `test/e2e/rollback.test.ts`. Cover: (1) Migration auto-captures snapshot, (2) runDown reverses table creation (table gone after down), (3) runDown reverses column addition (column gone after down), (4) runDown reverses index addition, (5) runDown reverses trigger creation, (6) runDown reverses RLS enable, (7) runDown reverses grant, (8) Irreversible operations skipped gracefully (alter_column, add_enum_value), (9) Snapshot deleted after successful down, (10) Multiple migrations -> down only reverses the latest. Verify DB state after each down.

## Produces

- `test/e2e/rollback.test.ts`

## Completion Notes

Created 10 E2E tests covering the full rollback lifecycle: snapshot auto-capture, runDown reversals for table creation, column addition, index addition, trigger creation, RLS enable, and grants. Also tests irreversible operation skipping, snapshot deletion after successful down, and multi-migration down-only-latest behavior.
