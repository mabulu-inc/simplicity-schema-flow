# T-064: E2E: Roles and extensions

- **Status**: TODO
- **Milestone**: 20 — E2E Tests: Schema Object Types
- **Depends**: T-059
- **PRD Reference**: §4.6, §4.7

## Description

Create `test/e2e/roles-extensions.test.ts`. Roles: (1) Create role with all attributes (login, superuser=false, createdb, createrole, inherit, bypassrls, replication, connection_limit), (2) Role group membership (`in: [group]`), (3) Role comment, (4) Role attribute change (alter), (5) Multiple roles with memberships. Extensions: (6) Create extension (pgcrypto or uuid-ossp), (7) Extension schema_grants, (8) Extension drop blocked without --allow-destructive. Verify by querying pg_roles, pg_auth_members, pg_extension.

## Produces

- `test/e2e/roles-extensions.test.ts`
