# T-074: E2E: Destructive operation blocking

- **Status**: DONE
- **Milestone**: 23 — E2E Tests: Safety & Zero-Downtime
- **Depends**: T-059
- **PRD Reference**: §8.1

## Description

Create `test/e2e/destructive-blocking.test.ts`. Verify all PRD §8.1 operations are blocked by default: (1) drop_table, (2) drop_column, (3) drop_index, (4) drop_foreign_key, (5) drop_view, (6) drop_materialized_view, (7) drop_extension, (8) disable_rls, (9) drop_policy, (10) drop_trigger, (11) type narrowing (text -> varchar(50)), (12) enum value removal. For each: run migration expecting blocked result, then re-run with allowDestructive=true expecting success. Use plan results to verify blocked operations count.

## Produces

- `test/e2e/destructive-blocking.test.ts`

## Completion

- **Completed**: 2026-03-10
- **Cost**: $35.21
- **Commit SHA**: 1abe2f4
- **Completion Notes**: 23 E2E tests covering all 12 PRD §8.1 destructive operation types. Each blocked operation verified with and without --allow-destructive (except type narrowing which verifies it passes through unblocked). Fixed pipeline edge case where empty schema dirs skip migration entirely by using anchor tables in drop_table and drop_extension tests.
