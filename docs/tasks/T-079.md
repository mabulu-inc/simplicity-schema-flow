# T-079: E2E: Lint rules

- **Status**: DONE
- **Milestone**: 24 — E2E Tests: Pipeline & Analysis
- **Depends**: T-059
- **PRD Reference**: §9
- **Completed**: 2026-03-10
- **Commit SHA**: 640d188
- **Completion Notes**: Added 9 E2E tests covering all 8 lint rules plus result structure validation. Fixed rename-detection rule to compare table portion of objectName (column ops use table.column format). Fixed regex patterns for quoted identifiers in FK column extraction, index column matching, and column name extraction.

## Description

Create `test/e2e/lint.test.ts`. Cover all 8 lint rules: (1) drop-table -- plan that drops a table triggers warning, (2) drop-column -- plan that drops column triggers warning, (3) set-not-null-direct -- (verify safe pattern doesn't trigger, or verify direct SET NOT NULL does trigger), (4) add-column-with-default -- column with volatile default triggers warning, (5) type-narrowing -- text->varchar triggers warning, (6) type-change -- integer->bigint triggers warning, (7) missing-fk-index -- FK column without index triggers info, (8) rename-detection -- drop+add same type triggers info. Verify lint result structure and severity.

## Produces

- `test/e2e/lint.test.ts`
