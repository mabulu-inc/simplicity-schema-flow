# T-065: E2E: Foreign keys

- **Status**: DONE
- **Completed**: 2025-07-10
- **Cost**: $16.58
- **Commit SHA**: 0d933c9
- **Milestone**: 21 — E2E Tests: Constraints, Indexes & Security
- **Depends**: T-059
- **PRD Reference**: §4.1

## Description

Create `test/e2e/foreign-keys.test.ts`. Cover: (1) Basic FK (references table.column), (2) FK with on_delete CASCADE, (3) FK with on_delete SET NULL, (4) FK with on_update CASCADE, (5) FK deferrable: true, initially_deferred: true, (6) Custom FK constraint name, (7) Cross-schema FK reference (if T-057 done), (8) FK added as NOT VALID then validated (verify constraint exists and is validated), (9) FK drop blocked without --allow-destructive. Verify by querying pg_constraint.

## Produces

- `test/e2e/foreign-keys.test.ts`

## Completion Notes

All 9 test cases implemented and passing:

1. Basic FK (references table.column) — verifies pg_constraint
2. FK with on_delete CASCADE — verifies confdeltype = 'c'
3. FK with on_delete SET NULL — verifies confdeltype = 'n'
4. FK with on_update CASCADE — verifies confupdtype = 'c'
5. FK deferrable + initially_deferred — verifies condeferrable/condeferred
6. Custom FK constraint name — verifies conname matches
7. Cross-schema FK reference — verifies ref_schema via pg_namespace
8. FK NOT VALID then validated — verifies convalidated = true
9. FK column drop blocked without --allow-destructive — two-migration test
