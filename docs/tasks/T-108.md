# T-108: Fix audit vulnerability and upgrade dependencies

- **Status**: TODO
- **Milestone**: 32 — Supply Chain Security
- **Depends**: none
- **PRD Reference**: §1 (Overview)
- **Complexity**: standard
- **GitHub Issue**: #5

## Description

Socket.dev shows a Supply Chain Security score of 78 for `@smplcty/schema-flow`. This task addresses the actionable items: fixing the audit vulnerability and upgrading outdated dependencies.

### Ralph must first act as product manager

1. Verify the audit vulnerability is real by running `pnpm audit`.
2. Verify the outdated dependencies by running `pnpm outdated`.
3. Comment on GitHub issue #5 with findings and plan of action.
4. Then proceed with the work.

### Implementation

- Run `pnpm update flatted` to resolve the high-severity `flatted` prototype pollution vulnerability (GHSA-rf6f-7fwh-wjgh). This is a lockfile-only fix since `^3.2.9` allows `>=3.4.2`.
- Upgrade patch/minor dependencies: `yaml`, `eslint`, `typescript-eslint`, `vitest`, `@vitest/coverage-v8`, `@types/pg`.
- **Upgrade TypeScript to 6.x** (required). Update `package.json` to `"typescript": "^6.0.2"` and fix any resulting type errors. This is a hard requirement, not optional.
- Run `pnpm check` to verify all lint, format, typecheck, build, and tests pass.
- Run `pnpm audit` to verify 0 vulnerabilities remain.

## Produces

- Updated `pnpm-lock.yaml` (and `package.json` for explicit version bumps)
- Clean `pnpm audit` output
- All quality gates pass
