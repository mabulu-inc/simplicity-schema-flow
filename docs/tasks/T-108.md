# T-108: Fix audit vulnerability and upgrade dependencies

- **Status**: DONE
- **Milestone**: 32 — Supply Chain Security
- **Depends**: none
- **PRD Reference**: §1 (Overview)
- **Complexity**: standard
- **GitHub Issue**: #5
- **Completed**: 2026-03-24 11:38 (5m duration)
- **Commit**: (pending)

## Description

Socket.dev shows a Supply Chain Security score of 78 for `@smplcty/schema-flow`. This task addresses the actionable items: fixing the audit vulnerability and upgrading outdated dependencies.

### Ralph must first act as product manager

1. Verify the audit vulnerability is real by running `pnpm audit`.
2. Verify the outdated dependencies by running `pnpm outdated`.
3. Comment on GitHub issue #5 with findings and plan of action.
4. Then proceed with the work.

### Implementation

- Run `pnpm update` to upgrade all dependencies to their latest semver-compatible versions. This also resolves the high-severity `flatted` prototype pollution vulnerability (GHSA-rf6f-7fwh-wjgh) since `^3.2.9` allows `>=3.4.2`.
- **Upgrade TypeScript to 6.x** (required). Use `pnpm up typescript@6` to bump the range and resolve. Fix any resulting type errors. This is a hard requirement, not optional.
- Do NOT manually edit `package.json` for specific dependency versions — use `pnpm update` / `pnpm up` commands and let the tooling manage version ranges.
- Run `pnpm check` to verify all lint, format, typecheck, build, and tests pass.
- Run `pnpm audit` to verify 0 vulnerabilities remain.

## Produces

- Updated `pnpm-lock.yaml` (and `package.json` only where pnpm commands update the range, e.g. TypeScript major bump)
- Clean `pnpm audit` output
- All quality gates pass

## Completion Notes

Upgraded all dependencies to latest semver-compatible versions and TypeScript to 6.0.2. Resolved high-severity flatted prototype pollution vulnerability (GHSA-rf6f-7fwh-wjgh). `pnpm audit` now shows 0 vulnerabilities. All 1078 tests pass across 64 test files. Commented findings and plan on GitHub issue #5.
