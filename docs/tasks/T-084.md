# T-084: E2E: Config resolution

- **Status**: TODO
- **Milestone**: 26 — E2E Tests: Configuration & Status
- **Depends**: T-059
- **PRD Reference**: §5

## Description

Create `test/e2e/config.test.ts`. Cover: (1) CLI flags override config file values, (2) Config file values override env vars, (3) Env vars override convention defaults, (4) --env selects environment block, (5) ${VAR} interpolation in config file, (6) Missing config file uses defaults, (7) lockTimeout and statementTimeout applied (verify via SHOW lock_timeout in session). Verify by inspecting resolved config or observing behavior.

## Produces

- `test/e2e/config.test.ts`
