# T-054: Column-level check sugar

- **Status**: DONE
- **Milestone**: 18 — Missing Feature Implementation
- **Depends**: T-006, T-009
- **PRD Reference**: §4.1
- **Completed**: 2026-03-10 10:03 (3m duration)
- **Commit**: ea5777b
- **Cost**: $6.46

## Description

Add `check` string field to ColumnDef (PRD §4.1). When a column has `check: "expr"`, the parser should generate a CheckDef with auto-generated name `chk_<table>_<column>` and the given expression. This is syntactic sugar -- the result is a normal check constraint on the table. Add parser tests and a planner test verifying the generated check appears in the plan.

## Produces

- Updated `src/schema/types.ts`
- Updated `src/schema/parser.ts`
- Tests

## Completion Notes

Added `check` optional string field to `ColumnDef` in types.ts. Updated `parseColumnDef` to parse the field and `parseTable` to generate `CheckDef` entries with auto-named `chk_<table>_<column>` for each column with check sugar. Column-level checks are appended after any explicit table-level checks. 3 new tests covering: single column check, merge with explicit checks, and multiple column checks. All 657 tests pass.
