# T-071: E2E: Seeds

- **Status**: DONE
- **Milestone**: 22 — E2E Tests: Mixins, Seeds, Prechecks, Expand
- **Depends**: T-059
- **PRD Reference**: §4.1
- **Completed**: 2026-03-10
- **Commit SHA**: (pending)
- **Completion Notes**: All 7 E2E seed scenarios covered: literal values, upsert re-run, DO NOTHING conflict, UUID PKs, null/boolean values, !sql expressions, and multiple rows.

## Description

Create `test/e2e/seeds.test.ts`. Cover: (1) Initial seed insert with literal values, (2) Seed upsert (re-run changes values), (3) seeds_on_conflict: "DO NOTHING" (if T-056 done -- insert-only, skip existing), (4) Seed with UUID primary key, (5) Seed with null and boolean values, (6) Seed with !sql expression (if T-056 done -- e.g., `!sql now()`), (7) Multiple seed rows. Verify by SELECT from seeded table.

## Produces

- `test/e2e/seeds.test.ts`
