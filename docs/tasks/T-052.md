# T-052: force_rls table attribute

- **Status**: DONE
- **Milestone**: 18 — Missing Feature Implementation
- **Depends**: T-009, T-010
- **PRD Reference**: §4.1, §4.8
- **Completed**: 2026-03-10 09:48
- **Commit**: 9ccc12d
- **Cost**: $68.51

## Description

Add `force_rls` boolean to TableSchema and MixinSchema (PRD §4.1, §4.8). When `force_rls: true`, the planner should emit `ALTER TABLE ... FORCE ROW LEVEL SECURITY` in addition to `ENABLE ROW LEVEL SECURITY`. Introspection must read `relforcerowsecurity` from `pg_class`. Drift must detect when force_rls differs. Parser must accept and validate the field. Update executor to handle the new SQL.

## Produces

- Updated `src/schema/types.ts`
- Updated `src/schema/parser.ts`
- Updated `src/planner/index.ts`
- Updated `src/executor/index.ts`
- Updated `src/introspect/index.ts`
- Updated `src/drift/index.ts`
- Tests

## Completion Notes

Added `force_rls` boolean across all layers: types, parser, planner (with `force_rls`/`disable_force_rls` operations), introspect (reads `relforcerowsecurity` from pg_class), drift (detects force_rls differences), and mixins (merges force_rls into consuming tables). Fixed a boolean coercion bug in `wantRls` computation (planner `diffRls` and drift `driftRls`) where `undefined` policies caused false drift reports. Executor needed no changes — it generically executes SQL from operations. 635 tests pass (11 new tests for force_rls).
