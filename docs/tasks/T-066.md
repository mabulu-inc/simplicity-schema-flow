# T-066: E2E: Indexes

- **Status**: DONE
- **Completed**: 2026-03-10
- **Cost**: $16.17
- **Commit SHA**: b10be53
- **Milestone**: 21 — E2E Tests: Constraints, Indexes & Security
- **Depends**: T-059
- **PRD Reference**: §4.1

## Description

Create `test/e2e/indexes.test.ts`. Cover: (1) Default btree index, (2) GIN index on jsonb, (3) GiST index, (4) Hash index, (5) BRIN index, (6) Partial index (WHERE clause), (7) Covering index (INCLUDE), (8) Opclass (text_pattern_ops), (9) Unique index, (10) Index with custom name, (11) Index comment, (12) Multiple indexes on one table, (13) Index drop blocked without --allow-destructive. Verify by querying pg_indexes, pg_class, pg_am.

## Completion Notes

All 13 test cases implemented and passing:

1. Default btree index
2. GIN index on jsonb
3. GiST index on tsvector
4. Hash index
5. BRIN index on timestamptz
6. Partial index with WHERE clause
7. Covering index with INCLUDE
8. Opclass (text_pattern_ops)
9. Unique index
10. Index with custom name
11. Index comment (required planner change to emit COMMENT ON INDEX)
12. Multiple indexes on one table
13. Index drop blocked without --allow-destructive

Added index comment support to planner (createTableOps and diffIndexes).

## Produces

- `test/e2e/indexes.test.ts`
