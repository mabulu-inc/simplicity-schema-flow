# T-075: E2E: Zero-downtime patterns

- **Status**: TODO
- **Milestone**: 23 — E2E Tests: Safety & Zero-Downtime
- **Depends**: T-059
- **PRD Reference**: §8.3

## Description

Create `test/e2e/zdm-patterns.test.ts`. Cover: (1) Safe NOT NULL -- change column from nullable to non-nullable, verify 4-step pattern (CHECK NOT VALID -> VALIDATE -> SET NOT NULL -> DROP CHECK), column ends up NOT NULL, (2) Safe unique constraint -- add unique constraint to existing table, verify 2-step pattern (CREATE UNIQUE INDEX CONCURRENTLY -> ADD CONSTRAINT USING INDEX), (3) CONCURRENTLY indexes -- verify indexes created CONCURRENTLY are not wrapped in transactions (check SQL generation output), (4) FK NOT VALID -- add FK to existing table, verify added as NOT VALID then validated. Verify final DB state is correct for each pattern.

## Produces

- `test/e2e/zdm-patterns.test.ts`
