# T-081: E2E: Validate and baseline

- **Status**: TODO
- **Milestone**: 25 — E2E Tests: Commands & Generation
- **Depends**: T-059
- **PRD Reference**: §6.1

## Description

Create `test/e2e/validate-baseline.test.ts`. Validate: (1) validate runs migration in rolled-back transaction -- no objects exist after, (2) validate catches invalid SQL (returns error), (3) validate with valid schema succeeds. Baseline: (4) baseline records all files in history without executing migration (no tables created), (5) After baseline, running migration detects no changes (files already tracked), (6) baseline with --json outputs JSON.

## Produces

- `test/e2e/validate-baseline.test.ts`
