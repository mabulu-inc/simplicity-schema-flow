# T-076: E2E: Concurrency and recovery

- **Status**: TODO
- **Milestone**: 23 — E2E Tests: Safety & Zero-Downtime
- **Depends**: T-059
- **PRD Reference**: §8.2, §8.4

## Description

Create `test/e2e/concurrency-recovery.test.ts`. Cover: (1) Advisory lock -- migration acquires lock, verify lock exists during execution, (2) Transactional rollback -- migration with intentionally invalid SQL rolls back all changes (no partial state), (3) Hash-based change detection -- run same migration twice, second run produces no operations, (4) Changed file re-run -- modify YAML content, second run detects and applies change, (5) Invalid index detection and cleanup -- create invalid index manually, verify detectInvalidIndexes finds it, reindexInvalid fixes it. (6) Retry on transient errors -- if feasible to test, verify retry behavior on lock timeout.

## Produces

- `test/e2e/concurrency-recovery.test.ts`
