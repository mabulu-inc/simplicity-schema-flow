# T-098: Document view options and view triggers

- **Status**: DONE
- **Completed**: 2026-03-18T13:00:00Z
- **Commit**: cde30a760eb13d761b0d69fa086b01b1c0c6ed03
- **Cost**: $0.24
- **Completion Notes**: Updated docs-site views page with options and triggers fields in YAML example and Fields table, added View options subsection (security_barrier, security_invoker, check_option), and View triggers subsection with INSTEAD OF trigger examples. README has no views YAML example so no change needed.
- **Milestone**: 29 — View Enhancements
- **Depends**: T-097
- **PRD Reference**: §4.4
- **Complexity**: light
- **Touches**: `README.md`, `docs-site/src/content/docs/schema/views.md`

## Description

T-096 and T-097 added view options (`WITH` clause) and INSTEAD OF trigger support but did not update the README or docs-site. The docs-site views page (`docs-site/src/content/docs/schema/views.md`) is the primary gap — it has no mention of `options` or `triggers` on views.

## Changes

1. **`docs-site/src/content/docs/schema/views.md`** — Update the regular views YAML example to include `options` and `triggers`. Add `options` and `triggers` to the Fields table. Add a "View options" subsection explaining `security_barrier`, `security_invoker`, and `check_option`. Add a "View triggers" subsection explaining INSTEAD OF triggers with an example.
2. **`README.md`** — If the README has a views YAML example, add `options` and `triggers` to it. If not, no change needed.

## AC

- Docs-site views page shows `options` field in the YAML example and Fields table
- Docs-site views page shows `triggers` field in the YAML example and Fields table
- Docs-site views page has subsections explaining view options and INSTEAD OF triggers
- README is updated only if it contains a views YAML example
- `pnpm check` passes
