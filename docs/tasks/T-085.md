# T-085: E2E: Status command and multi-run behavior

- **Status**: TODO
- **Milestone**: 26 — E2E Tests: Configuration & Status
- **Depends**: T-059
- **PRD Reference**: §6.2, §12

## Description

Create `test/e2e/status-multirun.test.ts`. Status: (1) status on fresh DB shows no applied files, (2) status after migration shows applied files with hashes, (3) status after file change shows pending changes. Multi-run: (4) Idempotent re-run -- same YAML produces no operations on second run, (5) Additive change -- add column to existing table, second run adds only that column, (6) Full lifecycle -- create -> modify -> verify -> drift clean across 3 runs.

## Produces

- `test/e2e/status-multirun.test.ts`
