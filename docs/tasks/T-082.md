# T-082: E2E: SQL and ERD generation

- **Status**: TODO
- **Milestone**: 25 — E2E Tests: Commands & Generation
- **Depends**: T-059
- **PRD Reference**: §11.4, §11.5

## Description

Create `test/e2e/generation.test.ts`. SQL: (1) generateSql produces valid SQL string with BEGIN/COMMIT blocks, (2) CONCURRENTLY operations appear outside transaction blocks, (3) Blocked operations appear as comments, (4) SQL file written to disk with --output. ERD: (5) generateErd produces valid Mermaid syntax, (6) Tables with columns and types rendered, (7) FK relationships rendered as edges, (8) Composite PKs rendered correctly, (9) ERD written to file with --output.

## Produces

- `test/e2e/generation.test.ts`
