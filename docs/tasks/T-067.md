# T-067: E2E: Check and unique constraints

- **Status**: DONE
- **Completed**: 2026-03-10
- **Commit SHA**: 6d2b3fb
- **Milestone**: 21 — E2E Tests: Constraints, Indexes & Security
- **Depends**: T-059
- **PRD Reference**: §4.1

## Description

Create `test/e2e/constraints.test.ts`. Cover: (1) Named table-level check constraint, (2) Check constraint with comment, (3) Column-level check sugar (if T-054 done), (4) Multi-column unique constraint, (5) Unique constraint with custom name, (6) Unique constraint with comment, (7) Check constraint drop blocked without --allow-destructive, (8) Unique constraint drop blocked without --allow-destructive. Verify by querying pg_constraint.

## Completion Notes

All 8 test cases implemented and passing:

1. Named table-level check constraint
2. Check constraint with comment
3. Column-level check sugar (T-054 done)
4. Multi-column unique constraint
5. Unique constraint with custom name
6. Unique constraint with comment
7. Check constraint drop blocked without --allow-destructive
8. Unique constraint drop blocked without --allow-destructive

Added comment support for check and unique constraints in the planner (both create and diff paths). Also added drop_check detection in diffChecks for destructive protection.

## Produces

- `test/e2e/constraints.test.ts`
