# T-063: E2E: Views and materialized views

- **Status**: TODO
- **Milestone**: 20 — E2E Tests: Schema Object Types
- **Depends**: T-059
- **PRD Reference**: §4.4, §4.5

## Description

Create `test/e2e/views.test.ts`. Cover: (1) Regular view creation with query, (2) View grants, (3) View comment, (4) View query change triggers recreate, (5) Materialized view creation, (6) Materialized view indexes, (7) Materialized view grants, (8) Materialized view comment, (9) Materialized view query change triggers refresh, (10) Materialized view with data verification (query returns rows). Verify by querying information_schema.views, pg_matviews, and selecting from the views.

## Produces

- `test/e2e/views.test.ts`
