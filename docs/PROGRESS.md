# Progress Log

## Current State

<!-- Updated by each Ralph Loop iteration. Read this FIRST. -->

Last completed task: T-041 (Function options in SQL generation)
Next eligible task: T-042 (Trigger for_each and when clause)

## Completed Tasks

- **T-001**: Project setup and config system — TypeScript project (tsconfig, vitest, pnpm), config resolution with defaults/env/yaml/CLI overrides. 16 tests passing.
- **T-002**: Database connection management — Pool singleton, withClient, withTransaction, retry logic for transient errors, configurable timeouts, testConnection. 19 tests passing.
- **T-000**: Docker-based test database setup — docker-compose.yml with PG 17 on port 54329 (tmpfs, healthcheck), .env/.env.example, vitest loads dotenv, db.test.ts uses DATABASE_URL without fallback. 35 tests passing.
- **T-003**: File discovery and tracking — glob-based YAML/SQL discovery from schema dir with phase classification (pre/schema/post), SHA-256 hash-based file tracker using \_simplicity.history table, change detection via hash comparison. 62 tests passing.
- **T-004**: Logger — Structured leveled logger (debug/info/warn/error) with colored output, verbose/quiet/json modes, configurable output streams. 15 tests passing.
- **T-005**: Schema type definitions — All YAML schema types defined in `src/schema/types.ts`: TableSchema, ColumnDef, IndexDef, CheckDef, UniqueConstraintDef, TriggerDef, PolicyDef, FunctionSchema, FunctionArg, EnumSchema, ViewSchema, MaterializedViewSchema, RoleSchema, ExtensionsSchema, MixinSchema, GrantDef, FunctionGrantDef, PrecheckDef, and supporting types. 77 tests passing (no regressions).
- **T-006**: YAML parser — `src/schema/parser.ts` with parsers for all schema kinds (table, enum, function, view, materialized view, role, extensions, mixin). Validates required fields, validates enum values (timing, method, security, etc.), applies defaults. `parseSchemaFile` auto-detects kind from YAML content. 38 new tests, 115 total passing.
- **T-007**: Mixin system — `src/schema/mixins.ts` with `loadMixins` (builds name→MixinSchema registry, rejects duplicates) and `applyMixins` (merges columns/indexes/checks/triggers/policies/grants into table schemas, skips duplicate columns, substitutes `{table}` placeholder in all string fields). Immutable — does not mutate inputs. 19 new tests, 134 total passing.
- **T-008**: Database introspection — `src/introspect/index.ts` with `getExistingTables`, `getExistingEnums`, `getExistingFunctions`, `getExistingViews`, `getExistingMaterializedViews`, `getExistingRoles`, and `introspectTable` (columns with types/nullability/defaults/PKs, indexes, check constraints, foreign keys, triggers, RLS policies, table/column comments). All queries use pg_catalog for accuracy. 16 new tests, 150 total passing.
- **T-009**: Planner / diff engine — `src/planner/index.ts` with `buildPlan` that compares desired (YAML) state vs actual (introspected) state. Produces ordered operations across all object types (extensions, enums, roles, functions, tables, columns, indexes, FKs, views, materialized views, triggers, RLS policies, grants, comments, seeds). FKs added as NOT VALID then validated. Destructive operations blocked unless allowDestructive set. Operations sorted by phase for correct dependency order. 49 new tests, 199 total passing.
- **T-010**: Executor — `src/executor/index.ts` with `execute` function that runs planned operations in phased order within transactions. Advisory locking (pg_try_advisory_lock) prevents concurrent migrations. Dry-run mode logs operations without executing. Validate mode executes in a rolled-back transaction. Pre/post SQL scripts run in separate transactions with SHA-256 hash tracking (skip unchanged files). Automatic rollback on errors. Ensures `_simplicity` schema exists. 12 new tests, 211 total passing.
- **T-011**: CLI entry point — `src/cli/index.ts` with argument parsing (`src/cli/args.ts`), help text (`src/cli/help.ts`), and pipeline orchestration (`src/cli/pipeline.ts`). Commands: run (full/pre/migrate/post), plan (dry-run), validate (rollback transaction), status (applied files + pending changes), init (create directory structure), help, version. Full pipeline wiring: discover → parse → expand mixins → introspect DB → plan → execute. 35 new tests, 246 total passing.
- **T-012**: Drift detection — `src/drift/index.ts` with `detectDrift` function. Structured `DriftReport` covering all object types. 31 tests, 277 total passing.
- **T-013**: Scaffold / generate — `src/scaffold/index.ts` with `generateFromDb` (introspects DB objects and produces YAML files, one per table/enum/function/view/role), `scaffoldInit` (creates standard directory structure), `scaffoldPre`/`scaffoldPost` (timestamped SQL templates), `scaffoldMixin` (YAML mixin template). Writes files to disk when outputDir provided. 15 new tests, 292 total passing.
- **T-014**: Rollback — `src/rollback/index.ts` with `ensureSnapshotsTable` (creates `_simplicity.snapshots`), `saveSnapshot`/`getLatestSnapshot`/`listSnapshots`/`deleteSnapshot` (snapshot CRUD), `computeRollback` (computes reverse operations from a snapshot — handles create_table→DROP, add_column→DROP COLUMN, create_enum→DROP TYPE, add_index→DROP INDEX, create_function→DROP FUNCTION, create/drop trigger/policy/view/materialized_view, enable_rls→DISABLE, constraints, grants, extensions, roles; skips irreversible ops like alter_column/add_enum_value), `runDown` (loads latest snapshot, executes reverse operations in transaction with advisory locking, deletes snapshot on success). 29 new tests, 321 total passing.
- **T-015**: Expand/contract — `src/expand/index.ts` with `ensureExpandStateTable` (creates `_simplicity.expand_state`), `planExpandColumn` (generates operations: add column, create dual-write trigger function + trigger, backfill UPDATE), `runBackfill` (batch backfill with configurable batch size using ctid-based batching), `runContract` (drops old column, trigger, and trigger function within advisory-locked transaction; updates state to 'contracted'), `getExpandStatus` (lists all expand/contract states). Full lifecycle tested: expand → dual-write trigger fires on INSERT/UPDATE → backfill → contract drops old column. 10 new tests, 331 total passing.
- **T-017**: Lint — `src/lint/index.ts` with `lintPlan` function. Implements all 8 PRD lint rules: `drop-table`, `drop-column`, `set-not-null-direct` (with safe CHECK pattern detection), `add-column-with-default` (volatile default detection), `type-change`, `type-narrowing`, `missing-fk-index` (with index coverage check), `rename-detection` (drop+add on same table). Returns structured `LintResult` with warnings and severity summary. Lints both executed and blocked operations. 16 new tests, 358 total passing.
- **T-018**: ERD generation — `src/erd/index.ts` with `generateErd` function that produces Mermaid ER diagrams from parsed TableSchema arrays. Renders tables with columns (type, name, PK/FK markers, comments), generates FK relationships as edges with correct cardinality (one-to-many, zero-or-many for nullable FKs, one-to-one for unique FKs). Supports composite primary keys. 11 new tests, 369 total passing.
- **T-019**: Public API surface — `src/index.ts` barrel file re-exporting all public functionality: core (config, db, files, logger), schema parsing (parser, mixins, types), introspection, planner, executor, drift, lint, rollback, expand/contract, scaffold/generate, SQL generation, ERD generation, CLI pipeline. All types exported. 14 new tests, 383 total passing.
- **T-020**: Test infrastructure — `src/testing/index.ts` with `useTestProject` (creates isolated PG schema + temp directory, returns config/migrate/drift/cleanup helpers) and `writeSchema` (writes YAML files to temp dirs with auto-created subdirectories). Full integration tests verify schema isolation, cleanup, migration pipeline, and drift detection. 6 new tests, 389 total passing.
- **T-022**: Safe NOT NULL pattern — Planner now produces 4-step safe pattern for nullable→non-nullable: (1) ADD CHECK NOT VALID, (2) VALIDATE CONSTRAINT, (3) SET NOT NULL, (4) DROP redundant check. New operation types `add_check_not_valid` and `drop_check`. Lint updated to recognize safe pattern. Executor integration test verifies all 4 steps execute against real PG. 5 new tests, 402 total passing.
- **T-024**: Enum value removal blocking — Planner now detects removed enum values and produces `remove_enum_value` operations marked destructive. Blocked by default, allowed with `--allow-destructive`. 3 new tests, 411 total passing.
- **T-023**: Safe unique constraint pattern — Planner produces 2-step safe pattern for adding unique constraints to existing tables: (1) CREATE UNIQUE INDEX CONCURRENTLY, (2) ALTER TABLE ADD CONSTRAINT ... USING INDEX. Both operations marked concurrent for outside-transaction execution. Added diffUniqueConstraints to handle add/drop of unique constraints on existing tables. Drop is destructive and blocked by default. 5 new planner tests + 1 executor integration test, 408 total passing.
- **T-025**: Grant/revoke for sequences and functions — Added `grant_function`, `revoke_function`, `grant_sequence`, `revoke_sequence` operation types. Function grants from YAML `grants` field now produce `grant_function` operations with correct function signature. Auto-generated `grant_sequence` for tables with serial/bigserial columns when grants include write privileges (INSERT/UPDATE/ALL). 6 new planner tests + 2 executor integration tests, 421 total passing.
- **T-026**: Role group memberships — Added `grant_membership` operation type. Planner produces `GRANT group TO role` operations when role has `in: [group_name]` field. Introspection now reads role memberships from `pg_auth_members`. Added `connection_limit` diffing to `diffRoleAttributes`. 7 new planner tests (membership, all attributes, connection_limit) + 2 executor integration tests (grant_membership, alter_role with attributes), 430 total passing.
- **T-027**: Materialized view grants, comments, and refresh
- **T-029**: Baseline command — Added `baseline` CLI command with `runBaseline` pipeline function. Records all discovered schema files (pre/schema/post) in `_simplicity.history` without running migrations. CLI arg parsing, dispatch, and JSON output support. 3 new tests, 442 total passing.
- **T-030**: Missing CLI command parsing — Added argument parsing and CLI wiring for all missing commands: drift (with --apply), lint, generate (with --output-dir, --seeds), sql (with --output), erd (with --output), new pre/post/mixin (with --name), down, contract, expand-status, docs. Added buildDesiredAndActual and getPlan pipeline helpers. 20 new tests, 462 total passing.
- **T-031**: Precheck execution and abort — Added `run_precheck` operation type to planner. Planner generates precheck operations (phase 0) from table `prechecks` field for both new and existing tables. Executor runs prechecks before main operations, aborting with the configured message when query returns falsy. 3 planner tests + 4 executor integration tests, 469 total passing.
- **T-032**: Auto snapshot capture during migration
- **T-033**: Drift detection completeness — Added 13 new tests and implementation for all missing drift comparison dimensions: index attribute differences (uniqueness, method, where, columns), FK constraint drift, unique constraint drift, function args comparison, role membership drift, grant drift (table-level), and seed drift. 490 total tests passing. — Executor now auto-saves a `MigrationSnapshot` to `_simplicity.snapshots` before executing operations (skipped for dry-run and empty plans). Pipeline passes `pgSchema` to executor. `runDown` works against auto-captured snapshots. 4 new tests, 473 total passing.
- **T-034**: PRD-listed API exports — Added all missing PRD §13 exports to `src/index.ts`: pipeline convenience functions (`runAll`, `runPre`, `runMigrate`, `runPost`, `runValidate`, `runBaseline`), file-path-based parsers (`parseTableFile`, `parseFunctionFile`, `parseEnumFile`, `parseViewFile`, `parseRoleFile`), and SQL helpers (`generateSqlFile`, `formatMigrationSql`). 3 new tests, 493 total passing.
- **T-035**: Generated columns — Introspection now detects generated column expressions via `is_generated`/`generation_expression` from `information_schema.columns`. Drift detection compares `generated` field on columns (reports missing, different, or extra). Planner already supported `GENERATED ALWAYS AS (expr) STORED` in CREATE TABLE and ADD COLUMN. 9 new tests (2 planner, 1 executor integration, 2 introspect, 4 drift), 502 total passing.
- **T-037**: Composite primary keys — Introspection now detects composite PKs via pg_constraint and sets `primary_key` array on TableSchema (only for multi-column PKs). Drift detection compares composite PK structure (missing, different, extra). Planner already supported composite PKs in CREATE TABLE. 9 new tests (2 planner, 1 executor integration, 2 introspect, 4 drift), 520 total passing.
- **T-038**: Seed upsert execution — Added 4 executor integration tests verifying seed INSERT ... ON CONFLICT upsert operations execute against real PostgreSQL: initial insert, update-on-conflict with new rows, UUID primary keys, null/boolean value handling. 524 total passing.
- **T-039**: Foreign key options in SQL generation — Planner now includes `deferrable`/`initially_deferred` in FK SQL (both new table and add-column paths). Introspection reads `condeferrable`/`condeferred` from pg_constraint and populates ForeignKeyRef. Drift detection compares `on_delete`, `on_update`, `deferrable`, and `initially_deferred` attributes. 3 planner tests, 2 executor integration tests, 1 introspect test, 3 drift tests. 533 total passing.
- **T-040**: Index options in SQL generation — Added opclass support to `createIndexOp`. Planner tests for gin, gist, hash, brin methods, WHERE (partial), INCLUDE (covering), opclass, and combined options. Executor integration tests verifying gin, partial, covering, and opclass indexes execute against real PG. 8 planner + 4 executor tests. 545 total passing.
- **T-041**: Function options in SQL generation — Planner now includes parallel, strict, leakproof, cost, rows, and set configuration parameters in function SQL. Introspection reads all function attributes from pg_proc including proconfig for SET parameters. Drift detection compares all new attributes. 6 planner tests, 2 executor integration tests, 2 introspect tests. 555 total passing.
- **T-036**: Column-level grants — Added `grant_column` and `revoke_column` operation types to planner. Planner now produces `grant_column` (not `grant_table`) for grants with `columns` field, generating `GRANT SELECT (col1, col2) ON table TO role` SQL. Introspection reads column-level privileges from `information_schema.column_privileges`, grouping by grantee and merging privileges. Drift detection includes column info in grant comparison keys. 9 new tests (2 planner, 1 executor integration, 4 drift, 2 introspect), 511 total passing.
- **T-028**: Extension schema_grants SQL generation — Added `grant_schema` operation type. Planner now produces `GRANT USAGE ON SCHEMA` operations from `schema_grants` in extensions YAML. Supports multiple schemas and roles. 3 new planner tests + 1 executor integration test verifying grant executes and `has_schema_privilege` confirms access. 439 total passing. — Parser test for materialized view with grants/comment fields. Planner now produces `refresh_materialized_view` operation after recreating a materialized view when query changes. Planner tests for grant_table, set_comment, and refresh_materialized_view operations on materialized views. Executor integration test verifying CREATE, REFRESH, GRANT, and COMMENT all execute correctly against real PG with data verification. 5 new tests, 435 total passing.
- **T-021**: CONCURRENTLY indexes — Planner generates `CREATE INDEX CONCURRENTLY` SQL with `concurrent` flag on operations. Executor splits concurrent ops out of the main transaction, executing them individually outside BEGIN/COMMIT. SQL generator places concurrent ops outside transaction blocks with dedicated section labels. 8 new tests, 397 total passing. — `src/testing/index.ts` with `useTestProject` (creates isolated PG schema + temp directory, returns config/migrate/drift/cleanup helpers) and `writeSchema` (writes YAML files to temp dirs with auto-created subdirectories). Full integration tests verify schema isolation, cleanup, migration pipeline, and drift detection. 6 new tests, 389 total passing.
- **T-016**: SQL generation — `src/sql/index.ts` with `generateSql` function that renders a `PlanResult` as a standalone SQL string. Supports section comments by phase (Extensions, Enums, Tables, etc.), optional transaction wrapping (`wrapInTransaction`), optional blocked-operation comments (`includeBlocked`), header with generation timestamp, semicolon normalization. 11 new tests, 342 total passing. — `src/expand/index.ts` with `ensureExpandStateTable` (creates `_simplicity.expand_state`), `planExpandColumn` (generates operations: add column, create dual-write trigger function + trigger, backfill UPDATE), `runBackfill` (batch backfill with configurable batch size using ctid-based batching), `runContract` (drops old column, trigger, and trigger function within advisory-locked transaction; updates state to 'contracted'), `getExpandStatus` (lists all expand/contract states). Full lifecycle tested: expand → dual-write trigger fires on INSERT/UPDATE → backfill → contract drops old column. 10 new tests, 331 total passing.
